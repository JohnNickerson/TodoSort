@interface
	Allow multiple actions per invocation.
	Ability to cancel an action rather than confirming or selecting one search result.
	Search items.
	GUI
		- Search
		- Mark as done
		- Process inbox
		- Review Someday items
		- Add notes
		- Delete items
		- Defer items
		- Add follow-up actions
	Allow marking a project as "done" while processing, instead of needing a new action.
		- If action matches new context, just assign context.
	"Review" mode?
		- Process inbox and projects
		- Ask if each item is done or should be deferred
		- Process someday list and see if any were done or should be done now
	Keep processing while more items still in inbox or projects.
@gui
	Use a "go" button to separate the ticking of a box from the disk write, in case it is ticked accidentally.
@backend
	Selections of next actions, depending on the results of previous ones.
		- eg "Check something" then "|@shopping Buy this" and "|@home Set up that".
	Abstract out the serialiser codec so it can be replaced by, say, a YAML one.
		- Currently two static methods in the PIM library.
	Don't rewrite files if no changes are made.
		- Someday file already done.
		- Can't do for main list, because items need sorting.
		- Don't rewrite on "show" action.
	Tickler file
		- Automatic return to inbox from the Someday file on a certain date.
		- Specify another attribute on an action? (>2010-12-01)
		- Someday items with a date specified this way will be moved to the inbox after that date.
		- Any items in the todo list with dates should be deferred. Any in someday past their dates should be returned.
		- Automatically defer question-marked items?
	Generic CRUD operations on any list file
		- Move to another list file or sub-list, too
	Should lists always come in threes?
		- Defer, Do and Done.
		- That way they can have special meanings.
		- Or the list processor can just allow moving items between lists.
	Sync with Outlook? Google Tasks? Astrid?
	Add a timestamp when items are processed through the inbox.
	Change to mostly manage an offline someday/maybe file and move items to and from Google Tasks.
	Detect "[x]" marks as completed items
@bugs
	Assumes the existence of todo.txt.
		- It should be created if it doesn't exist yet.
@done
	The Done list should be broken up into date sub lists rather than being a different format.

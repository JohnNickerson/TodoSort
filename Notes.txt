@command-line
	Allow multiple actions per invocation.
	Allow marking a project as "done" while processing, instead of needing a new action.
		- If action matches new context, just assign context.
	Review mode?
		- Process inbox and projects
		- Ask if each item is done or should be deferred
		- Process someday list and see if any were done or should be done now
	Keep processing while more items still in inbox or projects.
	Open any list file rather than just the preconfigured ones
	Ask for a tickle date when deferring items
		- Can be null.
	Detailed help on each command
		- Eg "todosort help defer" to bring up a page just on the "defer" command.
		- This would allow detailed help when command parsing fails.
	Write a command line argument parser
		- To turn a line of text arguments into a command and a list of argument pairs.
		- I could use a library, but by then I might be going a bit too far down the CLI rabbit hole.
	Make the CLI work on a single file only
		- Take an argument for the file name to open.
	Make "--head" a ViewModel option
		- Currently works after the fact in the PrintItems method.
	Change the disambiguation process to demand a single match
		- Tough for short words that might appear everywhere. Allow IDs?
@gui
	Search via GUI
	Mark as done via GUI
	Process inbox via GUI
	Review Someday items via GUI
	Add notes via GUI
	Delete items via GUI
	Defer items via GUI
	Add follow-up actions via GUI
	Rank items for priority via GUI
	Add/edit tags via GUI
	Use a "go" button to separate the ticking of a box from the disk write, in case it is ticked accidentally.
		- Or just don't clear the item immediately so that it can be unticked if required.
		- Or include an "undo" action.
@backend
	Selections of next actions, depending on the results of previous ones.
		- eg "Check something" then "|@shopping Buy this" and "|@home Set up that".
	Tickler file
		- Automatic return to inbox from the Someday file on a certain date.
		- Specify as a tag (#tickle-date:2010-12-01)
		- Someday items with a date specified this way will be moved to the inbox after that date.
	Generic CRUD operations on any list file
		- Move to another list file or sub-list, too
	Sync with external services
		- Outlook, Google Tasks, Wunderlist, Google Keep?
	Add a timestamp when items are processed through the inbox.
	Detect "[x]" marks as completed items
	MVVM architecture
		- Splitting functionality and view will help the transition to GUI.
	Use a second ViewModel for reviewing the Someday list?
	Unit tests
	Extensible command framework?
		- Doesn't seem like a great idea, actually.
	Recurring reminders and items
		- "repeats" tags? That's just a way to serialise it.
	Deferred projects should take all child items with them
		- And un-deferring, too.
	Import function
		- To merge lists and add new items in bulk.
		- Optionally assign all to a new context or keep in their existing contexts.
	Only write IDs if they are useful
		- If IDs are not referenced anywhere (ie no projects and no priorities) then they're just taking up space.
	JoinSearch-style search queries
		- Not sure if the JoinSearch function will work over objects this way, but the parser should be helpful.
		- Might need new functions to search particular object properties like tags.
		- Or maybe LINQ via the command line?
	Export GraphViz source
		- Code just needs to be transplanted.
@bugs
@done
	Ability to cancel an action rather than confirming or selecting one search result.
	Ranking
	Add tags to an item
		- Should be easy: search, tag name, tag value. All that code is already written.
	Search notes and tags
		- Search not only titles but notes and tags, too.
	Allow tags to contain colons.
		- At the moment, this inhibits the ability to store URLs and file paths.
	Implement an "open-tag" command for items that contain a URL tag
		- todosort show-tag url --search "YouTube Watch Later" | start

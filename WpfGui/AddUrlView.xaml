<Window x:Class="AssimilationSoftware.TodoSort.WpfGui.AddUrlView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Add URL" SizeToContent="WidthAndHeight"
    FocusManager.FocusedElement="{Binding ElementName=UrlBox}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="3"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Padding" Value="2"/>
                <Setter Property="MinWidth" Value="80"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="3"/>
                <Setter Property="MinWidth" Value="50"/>
            </Style>
        </Grid.Resources>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="URL"/>
        <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding Url}" x:Name="UrlBox"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Title"/>
        <Button Grid.Row="1" Grid.Column="1" Content="Fetch" Command="{Binding FetchTitleCommand}"/>
        <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding Title}"/>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Command="{Binding OkCommand}" IsDefault="True"/>
            <Button Content="Cancel" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
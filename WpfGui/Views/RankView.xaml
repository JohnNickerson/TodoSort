<Window x:Class="AssimilationSoftware.TodoSort.WpfGui.Views.RankView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AssimilationSoftware.TodoSort.WpfGui.Views"
        mc:Ignorable="d" SizeToContent="WidthAndHeight"
        Title="Rank Items" MinHeight="200" MinWidth="500" MaxWidth="2000">
    <Window.InputBindings>
        <KeyBinding Command="{Binding Path=Vote1Command}" Key="D1" Modifiers="Control"/>
        <KeyBinding Command="{Binding Path=Vote2Command}" Key="D2" Modifiers="Control"/>
    </Window.InputBindings>
    <Grid>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="MaxWidth" Value="80"/>
                <Setter Property="MinWidth" Value="60"/>
                <Setter Property="Margin" Value="10"/>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition SharedSizeGroup="RankColumns"/>
            <ColumnDefinition SharedSizeGroup="RankColumns"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
            <TextBlock Text="{Binding Path=CurrentPair.Action1.Title}"/>
            <ListBox ItemsSource="{Binding Path=CurrentPair.Action1.Notes}" BorderBrush="Transparent"/>
            <ListBox ItemsSource="{Binding Path=CurrentPair.Action1.Tags}" BorderBrush="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="#"/>
                            <TextBlock Text="{Binding Key}"/>
                            <TextBlock Text=":"/>
                            <TextBlock Text="{Binding Value}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Vote" Margin="10" Command="{Binding Vote1Command}" ToolTip="Ctrl+1"/>
            <Button Content="Edit" Margin="10" Command="{Binding Path=CurrentPair.Action1.EditCommand}"></Button>
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
            <TextBlock Text="{Binding Path=CurrentPair.Action2.Title}"/>
            <ListBox ItemsSource="{Binding Path=CurrentPair.Action2.Notes}" BorderBrush="Transparent"/>
            <ListBox ItemsSource="{Binding Path=CurrentPair.Action2.Tags}" BorderBrush="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="#"/>
                            <TextBlock Text="{Binding Key}"/>
                            <TextBlock Text=":"/>
                            <TextBlock Text="{Binding Value}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Vote" Margin="10" Command="{Binding Vote2Command}" ToolTip="Ctrl+2"/>
            <Button Content="Edit" Margin="10" Command="{Binding Path=CurrentPair.Action2.EditCommand}"></Button>
        </StackPanel>

        <ProgressBar Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" MinHeight="20" Margin="5" Value="{Binding CurrentIndex}" Maximum="{Binding Path=RankPairs.Count}"/>

        <Button Grid.Row="3" Grid.Column="0" Content="&lt;&lt;" Margin="10" Command="{Binding BackCommand}"/>
        <Button Grid.Row="3" Grid.Column="1" Content="&gt;&gt;" Margin="10" Command="{Binding ForwardCommand}"/>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Margin="5" MinWidth="80" Command="{Binding OkCommand}"/>
            <Button Content="Cancel" Margin="5" MinWidth="80" Command="{Binding CancelCommand}" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

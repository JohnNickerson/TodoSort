<Window x:Class="AssimilationSoftware.TodoSort.WpfGui.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="ActionLists" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBox Name="SearchBox" Text="{Binding Path=SearchTerm}" Width="100" />
            <Button Content="Search" Width="50" />
        </StackPanel>
        <ListView Grid.Row="1" ItemsSource="{Binding Path=SearchResults}">
            <ListViewItem Content="{Binding Title}"></ListViewItem>
        </ListView>
	</Grid>
</Window>
